- Make sure to run the code in a non-blocking manner

- Parse the response packet data based on its name or type of data
Since the response packet does not have indication that know which command it responded. So the idea is to store the last command that was sent, and use it in switch function then make the data structure